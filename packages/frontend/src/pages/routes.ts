export enum PageRoutes {
  inbox = '/',
  inboxItem = '/inbox/:id',
  sent = '/sent',
  drafts = '/drafts',
  savedSearches = '/saved-searches',
  archive = '/archive',
  actors = '/actors',
  bin = '/bin',
}
